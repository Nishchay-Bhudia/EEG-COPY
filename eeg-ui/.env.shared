# Tracked (NOT gitignored) — values here follow the repo to every clone/pull,
# unlike .env (machine-specific: DATABASE_URL, SESSION_SECRET, etc). Meant for
# secrets you've decided should just work everywhere without manual setup.
# A personal .env value for the same key always wins (see dev-server.js).
GROQ_API_KEY=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
